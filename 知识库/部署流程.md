# 上线流程（部署步骤）

1. npm install 安装依赖（国内先设 ELECTRON_MIRROR）
2. npm run build 编译
3. npm run electron 启动桌面应用；或在设置页填 API Key
4. 发布安装包：npm run dist 产出 release/win-unpacked，打 tag 后 CI 自动发布
注意：发布前必须跑完五条测试防线（smoke/test:unit/test:ipc/check:codes/test:window）。
