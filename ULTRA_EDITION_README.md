# 🚀 agent-base (Ultra Edition / 特别版说明文档)

> **版本定位**：本版本命名为 **Ultra 版本 (Ultra Edition)**。此版本为针对高级工程师、高阶创作者与深度 AI 协作场景专门定制的**特定独立旗舰版**，包含全套沉浸式交互流光、高拟态物理组件、独立思维链胶囊系统以及四大深度调色艺术主题。
> **升级策略**：本版本为**独立专享版本，不走全局强制更新推送**，不影响普通日常通道的更新链，供需要全量高级功能与极端极致体验的开发者与用户自由下载与使用。

---

## 🌟 核心新特性与设计重构总览

### 一、 Claude Code 风格「流光思考胶囊 (Thought Capsule)」
针对大语言模型在进行深度长思维链（Deep Thinking / Reasoning）推理时，传统前端容易发生“正文被污染”、“缺乏运行反馈”、“长文本刷屏”与“不可折叠”的问题，Ultra 版本带来了彻底的交互革命：
1. **双轨数据流分流（Dual-Stream Protocol）**：
   - 底层 `OpenAICompatibleProvider` 会在 SSE 响应流中精准分离 `delta.reasoning_content`（思维护盾）与 `delta.content`（回答正文），杜绝思维流混杂污染 Markdown。
2. **极光旋转光环 (Aurora Spinner)**：
   - 模型在后台进行深度构思时，胶囊头部展示 `16px` 极光光环以 `0.85s` 匀速优雅旋转；思考结束平滑变轨为淡绿色（`✓`）完成徽章。
3. **0.1s 毫秒级动态秒表与 Token 计数器**：
   - 实时显示推理所用毫秒级时间（`思考中 (2.4s)...` -> `已深度思考（用时 4.8s）`），并结合中英分词动态显示消耗 Token。
4. **物理阻尼抽屉折叠 (Physics Accordion)**：
   - 思考结束后默认收起为紧凑胶囊，点击头部可流畅展开内凹的暗色思维终端，内含光标跟随与全量思考链路文本。
5. **一键复制思维链 (Copy Stream)**：
   - 胶囊头部配备一键复制按钮，轻按即可将完整的 AI 构思全过程复制到剪贴板。
6. **架构严守 ≤300 行铁律**：
   - 模块化封装于原生 ES 模块 `renderer/modules/thought-module.js`（119 行），纯原生运行，零编译负担。

---

### 二、 Claude 原版 WebGL2 极光火焰推理滑块 (Astraeus Range Slider)
告别简陋的下拉单选菜单，Ultra 版实装了来自 Claude Code 官方原版的流体物理动力学推理滑块卡片：
1. **五档弹性触感档位**：
   - `Flow` (极速敏捷) ➜ `Lite` (轻度推理) ➜ `Pro` (平衡推理) ➜ `Max` (深度推理) ➜ `Ultracode` (极限思考)。
2. **WebGL2 粒子流体模拟**：
   - 集成四通道渲染管线（流体速度场更新 ➜ 横向高斯模糊 ➜ 纵向高斯模糊 ➜ 色调映射合成），当滑块推至 88% 以上时自动点燃流光火焰！
3. **弹性磁力吸附与记忆存储**：
   - 拖拽松手瞬间以三次方贝塞尔曲线（Cubic Ease-Out）吸附至最近档位，并自动持久化存储。

---

### 三、 何惜专属艺术主题库（内置 6 套质感主题）
在软件左侧栏 **⚙「设置」->「外观」** 中，除了默认的浅色与深色玻璃外，Ultra 版本深度集成了 4 套全新调色的高级艺术主题，支持全局毫秒级动态变色与环境光辉光：
1. **银渐层流光透明玻璃 (`theme-silver-glass`)**：
   - 深邃蓝灰通透暗底、银白流光反光微光、28px 高通透毛玻璃、冷银蓝与星空紫流光光斑平滑漂移，黑白分明。
2. **古典羊皮卷米白 (`theme-parchment`)**：
   - 温润古籍羊皮纸米黄色底衬、深胡桃黑字（`#271c10`）、皮革印泥火漆金点缀，柔和极度护眼。
3. **素描手绘米白质感 (`theme-handdrawn`)**：
   - 温暖素描纸米白质地、速写铅笔纯黑线条与文字（`#0f1115`）、铅笔蓝重点强调，文创画册风。
4. **8-Bit 复古赛博像素 (`theme-pixel`)**：
   - 纯黑街机底色、经典 CRT 荧光像素电光绿（`#38ffaa`）、霓虹青蓝发光边框，硬核极客对比度达 18:1，自动加载 `PixelPress` 和 `PixelVT` 矢量像素字体！

---

### 四、 全能桌面级插件生态与官方市场入库
Ultra 版本正式上架并打包了 3 款高级扩展插件到 `registry/packages/`，并在 `registry/registry.json` 中登记了官方 SHA-256 校验哈希：
1. **全能系统管家 (`system-master`)**：
   - 全盘任意路径安全读写、Windows 真实桌面直投、PowerShell/CMD 全功能终端调度与整机性能全景扫描。
2. **M3E Canvas 画布设计器 (`m3e-canvas`)**：
   - Material 3 Expressive 可视化原型设计器，快速生成在线交互画板链接与 AI 提示词一键导出至桌面。
3. **何惜专属艺术主题包 (`theme-customizer`)**：
   - 支持通过自然语言对话（如“帮我切换到羊皮卷主题”）或插件工具调用实时变换软件视觉质感。

---

### 五、 底层稳定性、安全与架构加固
1. **完全访问模式（Full 模式）真正零打扰放行**：
   - 修复了此前在完全访问模式下普通工具依旧弹窗审批的问题；在 Full 模式下文件/命令静默快速执行，仅保留关键人机决策卡片（`ask-user`）。
2. **ABCD 人机决策卡片主题完全联动**：
   - 决策弹窗卡片完全绑定 CSS 变量系统，跟随浅色、深色、羊皮卷、像素风无缝切换。
3. **用户数据与安装目录物理隔离**：
   - 用户配置、API Key、自定模型列表与对话历史严格锁定在 `AppData/Roaming/agent-base/`，彻底告别“每次更新软件配置被清空”的历史痛点。
4. **工程健康与代码规范**：
   - 保持单文件严守 `<= 300 行` 铁律；
   - 14 套单元测试（129 个测试用例）、72 个 IPC 通道自检、全链路冒烟测试 **100% 全部通过**。

---

## 📦 如何获取与运行 Ultra 版本

- **源码分支**：`feat/ultra-edition`
- **构建命令**：
  ```bash
  # 1. 编译 TypeScript 代码
  npm run build
  # 2. 本地单机运行
  npm run electron
  # 3. 打包生成便携包与安装包
  npx electron-builder
  ```
- **安装包生成路径**：`release/` 目录下将生成 `agent-base-0.7.2-setup.exe` 与免安装便携版 `agent-base-0.7.2-portable.exe`。
